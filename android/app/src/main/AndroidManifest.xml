<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".LocalLlmService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="on-device LLM inference server" />
        </service>

        <!-- LiteRT-LM's GPU backend (Backend.GPU()) — not loaded yet (see
             docs/litertlm-android-adapter.md step 4), added now so the release/R8
             build is proven with these present. required="false" so a device
             without OpenCL still installs and LiteRT-LM's NPU->GPU->CPU fallback
             falls through to CPU instead of failing to install. -->
        <uses-native-library
            android:name="libvndksupport.so"
            android:required="false" />
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- @capacitor/camera's getPhoto(): declaring this is what makes the plugin
         actually prompt for camera permission at runtime — confirmed from its
         Android source (LegacyCameraFlow.checkCameraPermissions): an
         undeclared CAMERA permission is treated as "not required" and the
         plugin never asks, silently misbehaving instead of failing loudly.
         Gallery picking needs no permission of its own on API 29+ (this
         app's realistic target hardware) as long as saveToGallery is false,
         which src/lib/native/imagePicker.ts always passes. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Without this, Play Store infers camera hardware as required from the
         CAMERA permission alone and filters out camera-less devices (tablets,
         Chromebooks) that can still use the app fine via gallery picking. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
